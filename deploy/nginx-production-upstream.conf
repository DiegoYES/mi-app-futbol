# Se instala en /etc/nginx/conf.d/data-fut-upstream.conf al activar el pool.
upstream datafut_node {
    least_conn;
    server 127.0.0.1:3000 max_fails=3 fail_timeout=10s;
    server 127.0.0.1:3001 max_fails=3 fail_timeout=10s;
    keepalive 32;
}
